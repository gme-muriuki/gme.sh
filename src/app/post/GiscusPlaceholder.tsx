export function GiscusPlaceholder() {
  return (
    <section className="mt-16 rounded border border-rule border-dashed bg-paper-raised/30 p-6">
      <p className="font-mono text-[10px] uppercase tracking-[0.18em] text-ink-muted">
        comments
      </p>
      <p className="mt-2 text-sm italic text-ink-muted">
        Giscus discussion thread mounts here in production. The prototype shows
        the styled container only.
      </p>
    </section>
  )
}
