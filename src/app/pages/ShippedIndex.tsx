export default function ShippedIndex() {
  return (
    <article>
      <header>
        <p className="text-sm text-ink-muted font-mono uppercase tracking-wider">
          shipped
        </p>
        <h1 className="mt-2 text-3xl font-semibold tracking-tight">Shipped</h1>
        <p className="mt-3 max-w-prose text-ink-muted">
          Product launches and project releases. Distinct from essays — a release,
          not a blog post.
        </p>
      </header>
      <p className="mt-12 text-sm text-ink-muted">Nothing shipped yet.</p>
    </article>
  )
}
