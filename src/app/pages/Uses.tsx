import { IndexHeader } from '@/app/chrome/IndexHeader'

export default function Uses() {
  return (
    <article>
      <IndexHeader
        kicker="uses"
        title="Uses"
        dek="Tools, editor, hardware, dotfiles. Living document, not a brand-deal page."
      />
      <p className="text-sm text-ink-faint">Coming soon.</p>
    </article>
  )
}
