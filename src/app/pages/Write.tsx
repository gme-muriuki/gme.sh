import { IndexHeader } from '@/app/chrome/IndexHeader'

export default function Write() {
  return (
    <article>
      <IndexHeader
        kicker="write"
        title="Write"
        dek="Editor + live preview. Behind auth in production. Real editor wired in commit 16."
      />
    </article>
  )
}
