export default function Write() {
  return (
    <article>
      <header>
        <p className="text-sm text-ink-muted font-mono uppercase tracking-wider">
          write
        </p>
        <h1 className="mt-2 text-3xl font-semibold tracking-tight">Write</h1>
        <p className="mt-3 max-w-prose text-ink-muted">
          Editor + live preview. Behind auth in production. Wired in commit 14.
        </p>
      </header>
    </article>
  )
}
